import { RouteComponentProps } from '@reach/router';
import React from 'react';

export default function SearchComponent(props: RouteComponentProps) {
  return <div>Should contain the search component </div>;
}
